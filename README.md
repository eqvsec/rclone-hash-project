# rclone-hash-project
A repository containing CSV collections of rclone file hashes, broken down by operating system, architecture, and package name. Both the rclone binaries and the packages that contain them are hashed in SHA1 and SHA256.
